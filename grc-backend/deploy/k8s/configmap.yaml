apiVersion: v1
kind: ConfigMap
metadata:
  name: grc-platform-config
  namespace: grc-platform
  labels:
    app: grc-platform
    component: backend
data:
  NODE_ENV: production
  PORT: "5000"
  JWT_EXPIRE: "7d"
  LOG_LEVEL: info
  MONGODB_AUTH_SOURCE: admin
  MONGODB_REPLICA_SET: rs0
